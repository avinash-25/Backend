/**
 * Node.js basics
 * Intro of node
 * working with modules
 * File system operations
 * understanding HTTP module
 */

//! Node.js Bacics. &  intro of nodejs

//? its not a programming language not even technology and not framework and library too.

//? nodejs ia only JS runtime environment.
// Only js cannot make backend because js does not have functionlaity at initial time.

//rayn dahl thinks bu the use of js only we can develop backend.
// Then he extract the `v8` engine from google chrome because `v8` engine uses c++ to develop.
//? He develop the wrapper who takes js coe and do some edits and handover to `v8` engine then server will be developed.

// we will write a js code and this code recieves wrapper layer of and that code create a server with the help of v8 engine c++ modules

//? nodeJS is a js runtime environment.

//!

/**
 * npm is a platform where we can upload the own packages and also download 
 */

//! 